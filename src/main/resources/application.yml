spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ebanking
    username: postgres
    password: 123

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
